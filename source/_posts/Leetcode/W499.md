---
title: Leetcode W499 题解
date: 2026-4-27 10:00:00
tags:
  - LeetCode
  - 算法竞赛
categories:
  - 算法竞赛
---

比赛链接：[Leetcode Weekly Contest 499](https://leetcode.com/contest/weekly-contest-499)

| 题号 | 题目 | 难度 | 分类 | 关键知识点 |
| --- | --- | --- | --- | --- |
| A | Valid Elements in an Array | Easy | 线性扫描 | 前缀/后缀最大值 |
| B | Sort Vowels by Frequency | Medium | 字符串 / 排序 | 频率统计 + 自定义排序 |
| C | Minimum Operations to Make Array Non Decreasing | Medium | 贪心 | 相邻差分 |
| D | Maximum Sum of Alternating Subsequence With Distance at Least K | Hard | DP | 树状数组维护 DP 前缀最大值 |

<!-- more -->

## A - Valid Elements in an Array

**题意**

给定整数数组 `nums`，长度为 $n$。元素 `nums[i]` 合法当且仅当它严格大于左边所有元素，或严格大于右边所有元素。首尾元素一定合法。返回所有合法元素，保持原相对顺序。

**解题思路**

对每个位置 $i$ 需要知道：左边所有元素的最大值、右边所有元素的最大值。

预处理 `pre[i]` 为 $nums[0..i-1]$ 的最大值，`suf[i]` 为 $nums[i+1..n-1]$ 的最大值。遍历时若 $nums[i] > pre[i]$ 或 $nums[i] > suf[i]$ 则加入答案。由于 $nums[i] \ge 1$ 而 $pre[0]=suf[n-1]=0$，首尾元素自然满足。

**代码**

```cpp
class Solution {
public:
    vector<int> findValidElements(vector<int>& nums) {
        int n = nums.size();
        vector<int> pre(n, 0), suf(n, 0);
        for (int i = 1; i < n; i++) pre[i] = max(pre[i - 1], nums[i - 1]);
        for (int i = n - 2; i >= 0; i--) suf[i] = max(suf[i + 1], nums[i + 1]);
        vector<int> ans;
        for (int i = 0; i < n; i++) {
            if (nums[i] > pre[i] || nums[i] > suf[i])
                ans.push_back(nums[i]);
        }
        return ans;
    }
};
```

**复杂度分析**

时间复杂度：$O(n)$，空间复杂度：$O(n)$。

---

## B - Sort Vowels by Frequency

**题意**

给定小写字符串 `s`，只允许重新排列其中的元音字母（`a, e, i, o, u`），使得元音按出现频率降序排列；频率相同时按首次出现位置升序排列。返回修改后的字符串。

**解题思路**

元音只有 5 种，统计每个元音的出现次数 `cnt` 和首次出现位置 `fst`。将出现过的元音取出，按 `(cnt 大者在前，cnt 相同时 fst 小者在前)` 排序，再按对应次数展开成序列。最后从左到右扫描原串，在元音位置依次填入新序列即可。

**代码**

```cpp
class Solution {
public:
    bool vow(char c) {
        return c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u';
    }
    string sortVowels(string s) {
        int n = s.size();
        vector<int> cnt(26, 0), fst(26, n);
        for (int i = 0; i < n; i++) {
            if (vow(s[i])) {
                int x = s[i] - 'a';
                cnt[x]++;
                fst[x] = min(fst[x], i);
            }
        }
        string vs = "aeiou";
        vector<char> ord;
        for (char c : vs) {
            if (cnt[c - 'a'] > 0) ord.push_back(c);
        }
        sort(ord.begin(), ord.end(), [&](char x, char y) {
            int a = x - 'a', b = y - 'a';
            if (cnt[a] != cnt[b]) return cnt[a] > cnt[b];
            return fst[a] < fst[b];
        });
        string t;
        for (char c : ord) {
            int x = c - 'a';
            t.append(cnt[x], c);
        }
        int p = 0;
        for (int i = 0; i < n; i++) {
            if (vow(s[i])) s[i] = t[p++];
        }
        return s;
    }
};
```

**复杂度分析**

时间复杂度：$O(n)$，空间复杂度：$O(n)$。

---

## C - Minimum Operations to Make Array Non Decreasing

**题意**

给定数组 `nums`，一次操作可选择任意子数组并给其中每个元素加上任意正整数 $x$（代价为 $x$，不是 $x \times$ 长度）。要求通过若干次操作使数组变为非递减数组，最小化所有 $x$ 的总和。

**解题思路**

关键观察：若相邻 $nums[i] > nums[i+1]$，设下降量为 $d = nums[i] - nums[i+1]$，则最终数组中 $i+1$ 相对于 $i$ 必须多增加至少 $d$，因此每个下降位置至少贡献 $d$ 的必要代价。

下界可达到：对每个下降位置 $i$，对后缀 $[i+1, n-1]$ 增加恰好 $d$。后缀操作不破坏已修复的非递减关系。总代价正好等于所有相邻下降量之和。

**代码**

```cpp
class Solution {
public:
    long long minOperations(vector<int>& nums) {
        int n = nums.size();
        long long ans = 0;
        for (int i = 0; i + 1 < n; i++) {
            if (nums[i] > nums[i + 1])
                ans += nums[i] - nums[i + 1];
        }
        return ans;
    }
};
```

**复杂度分析**

时间复杂度：$O(n)$，空间复杂度：$O(1)$。

---

## D - Maximum Sum of Alternating Subsequence With Distance at Least K

**题意**

给定数组 `nums` 和整数 $k$，选出下标严格递增的子序列，满足相邻下标距离至少为 $k$，且选中数值严格交替（峰-谷-峰或谷-峰-谷）。子序列长度 $1$ 亦合法。求最大元素和。

**解题思路**

定义两种 DP 状态：
- $up_i$：以 $i$ 结尾、最后一步为上升的最大和
- $dn_i$：以 $i$ 结尾、最后一步为下降的最大和

转移时前驱 $j$ 必须满足 $j \le i-k$，且交替要求：
$$up_i = nums_i + \max\{dn_j \mid nums_j < nums_i, j \le i-k\}$$
$$dn_i = nums_i + \max\{up_j \mid nums_j > nums_i, j \le i-k\}$$

扫描时将位置 $i-k$ 的状态加入数据结构。需要快速查询「值小于 $x$ 的最大 $dn$」和「值大于 $x$ 的最大 $up$」，用两个树状数组分别维护。值域通过坐标压缩优化到 $O(n)$。

**代码**

```cpp
class Solution {
public:
    struct BIT {
        int n;
        vector<long long> tr;
        BIT(int n = 0) { init(n); }
        void init(int m) { n = m; tr.assign(n + 1, 0); }
        void upd(int x, long long v) {
            for (int i = x; i <= n; i += i & -i) tr[i] = max(tr[i], v);
        }
        long long qry(int x) {
            long long res = 0;
            for (int i = x; i > 0; i -= i & -i) res = max(res, tr[i]);
            return res;
        }
    };

    long long maxAlternatingSum(vector<int>& nums, int k) {
        int n = nums.size();
        vector<int> val = nums;
        sort(val.begin(), val.end());
        val.erase(unique(val.begin(), val.end()), val.end());
        int m = val.size();
        vector<int> rk(n);
        for (int i = 0; i < n; i++)
            rk[i] = lower_bound(val.begin(), val.end(), nums[i]) - val.begin() + 1;

        vector<long long> up(n), dn(n);
        BIT bl(m), bg(m);
        long long ans = 0;

        for (int i = 0; i < n; i++) {
            int j = i - k;
            if (j >= 0) {
                int x = rk[j];
                bl.upd(x, dn[j]);
                bg.upd(m - x + 1, up[j]);
            }
            int x = rk[i];
            up[i] = bl.qry(x - 1) + nums[i];
            int rx = m - x + 1;
            dn[i] = bg.qry(rx - 1) + nums[i];
            ans = max(ans, max(up[i], dn[i]));
        }
        return ans;
    }
};
```

**复杂度分析**

时间复杂度：$O(n \log n)$（含坐标压缩），空间复杂度：$O(n + m)$。

---

## 变形

1. **A / B**：若要求返回下标而非元素值，只需在遍历时记录下标。
2. **C**：若操作代价改为 $x \times$ 长度，则需用线段树维护区间增量，转化为标准的区间加、区间查询问题。
3. **D**：若去掉距离限制 $k$，可退化为 $O(n)$ 贪心；若 $k=1$，可直接在相邻位置转移；若加强为「距离恰好为 $k$」，则只需把前驱严格限制在 $i-k$ 而非 $i-k$ 及之前。
